{
  "name": "@peterson-benhame/skills-catalog",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/skills-catalog",
  "projectType": "library",
  "tags": [
    "scope:skills"
  ],
  "targets": {
    "generate-registry": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx packages/skills-catalog/src/generate-registry.ts",
        "cwd": "{workspaceRoot}"
      },
      "inputs": [
        "{projectRoot}/skills/**/*"
      ],
      "outputs": [
        "{projectRoot}/skills-registry.json"
      ]
    },
    "generate-data": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx run @peterson-benhame/skills-catalog:generate-registry",
        "cwd": "{workspaceRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "generate-registry"
      ],
      "options": {
        "command": "node -e \"const fs=require('fs'); const path=require('path'); const root='packages/skills-catalog'; const out=path.join(root,'dist'); fs.rmSync(out,{recursive:true,force:true}); fs.mkdirSync(out,{recursive:true}); fs.copyFileSync(path.join(root,'package.json'),path.join(out,'package.json')); fs.copyFileSync(path.join(root,'skills-registry.json'),path.join(out,'skills-registry.json')); fs.cpSync(path.join(root,'skills'),path.join(out,'skills'),{recursive:true});\"",
        "cwd": "{workspaceRoot}",
        "parallel": false
      },
      "inputs": [
        "{projectRoot}/package.json",
        "{projectRoot}/skills-registry.json",
        "{projectRoot}/skills/**/*"
      ],
      "outputs": [
        "{projectRoot}/dist"
      ]
    },
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"const fs = require('fs'); const skills = fs.readdirSync('packages/skills-catalog/skills', {withFileTypes: true}).filter(d => d.isDirectory()); console.log('✅ Found', skills.length, 'skill directories');\"",
        "cwd": "{workspaceRoot}"
      }
    },
    "security-scan": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx packages/skills-catalog/src/scan-skills.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}